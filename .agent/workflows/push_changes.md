---
description: Push all current changes to GitHub with a generated commit message
---

1. Check the status of the repository to see what has changed.
2. Add all changed files to the staging area.
3. Generate a concise and descriptive commit message based on the changes.
4. Commit the changes.
5. Push the changes to the `origin` remote on the `main` branch.
